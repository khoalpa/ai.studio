# M30 — Final Production Stabilization

API v1 is now discoverable through `GET /api/v1/contracts` and every JSON response
includes `X-Audio-Story-API: v1`. The contract lists the read, workflow, backup, and
release endpoints. Diagnostics and release readiness remain deterministic and local.

