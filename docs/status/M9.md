# Milestone M9 Status

Status: COMPLETE.

## Scope

M9 implements Stage 4 canonical `video_prompts.json` schema 1.2 and the
`VIDEO_PRODUCTION_RELEASE` package. It is read-only for every inherited Stage 3
member and does not call an image, audio or video generator.

## M9-0 decision

- ADR-0001 resolves absent Stage 4 audio to `NATIVE_DIALOGUE` while retaining
  canonical v3.16.13 byte-for-byte and keeping its compiler finding visible.
- Schema 1.2 shape is authoritative: 8 root/23 clip fields for native dialogue
  and 7 root/22 clip fields for ambience-only or silent output.

## Implementation evidence

- Added strict Stage 3 archive intake with archive SHA authority, CRC/security,
  manifest/member order, file hashes, package digest, story/report/visual
  binding, image dimensions, quality verdict and character/landscape/portrait
  set-digest recomputation.
- Added explicit enum-only configuration resolution and deterministic
  sentence-bound timeline derivation using `Decimal` and `ROUND_HALF_UP`.
- Long utterances are split into lossless contiguous word-offset spans with a
  maximum usable span of 7.950 seconds.
- Added deterministic derived scenes, character-only reference routing, stable
  voice records, continuity state, provider-neutral prompt composition and all
  three audio projections.
- Added strict field-order serialization, output self-digest, prompt/audio/avoid
  budgets, exact prompt duplicate detection, source hash/span/digest/reference
  recomputation and continuous-state checks.
- Added deterministic Stage 4 manifest and ZIP construction. Every Stage 3
  non-manifest member is retained byte-for-byte; `video_prompts.json` is placed
  before the optional anchor; reopen/safe-extract/postwrite checks are enforced.
- Targeted M9 tests: 9 passed. Full suite: 338 passed, 2 skipped with 88.52%
  branch coverage. Ruff format/lint and strict mypy over 79 source files pass;
  canonical hash and `git diff --check` pass.

## Production execution evidence

The authoritative M8 package reopens at archive SHA-256
`85e941ce77cd61826f30e06098118e3c2be29a3952b01f806e96ca82f8eb462e`
and package digest
`7ecd8f0428b75ffe6d17fe02530b73fa16dd3d2185fb7580c7efc1b0cfe133c4`.

The user selected `KEY_SCENES`. The Stage 4 production package was written to
`artifacts/m9-stage4-production/story.zip` with:

- archive SHA-256
  `d4193f41567639938a4216d40884950d570e6fcc8932dbd9849ff4d9811d9092`;
- package digest
  `a19b83a2e044f7ddfee97e2c865506bb92ca22b7ff33c8833595ce2ae9637f5f`;
- parent package digest
  `7ecd8f0428b75ffe6d17fe02530b73fa16dd3d2185fb7580c7efc1b0cfe133c4`;
- 28 archive members, with `video_prompts.json` in the canonical final position
  for this no-anchor package;
- 95 clips and 546 seconds (`00:09:06`) of planned video containers;
- `KEY_SCENES`, `LANDSCAPE_16_9`, `NATIVE_DIALOGUE` and
  `CHAINED_LAST_FRAME` resolved configuration;
- ZIP `testzip()` returning no failing member, all inherited Stage 3 member
  bytes exact, and Stage 4 validation status `PASS`.

No image, audio or video generator was called, and no network or model download
was used. M9 is closed; do not begin a later milestone without a separate
milestone plan and instruction.
