# Milestone M7 Status

Status: M7-A, M7-B, M7-C1, M7-C2a, M7-C2b, M7-D AND M7-E COMPLETE; M7-F NOT STARTED.

## Scope

M7 is Stage 2 ZONE. The reviewed scope and Definition of Done are recorded in
`docs/m7-stage2-zone-plan.md`. This checkpoint includes M7-A immutable input
and sidecar contracts plus M7-B deterministic ZONE planning and pre-call
compilation. It does not call an image adapter or create landscape candidates.

## Preflight result

- M6 closure and combined production smoke: PASS.
- Working tree before planning: clean.
- Canonical prompt hash and read-only attribute: PASS.
- Offline, loopback-only and one-GPU-heavy-job invariants: retained.
- Existing Stage 2 capsule fixtures for all profiles: available.
- M6 image transaction, OCR, typography, package lifecycle and recovery layers:
  reusable.
- CURRENT `visual_plan.json` schema 1.0 and `visual_bible.json` schema 2.0:
  implemented with strict field order and deterministic serializers.

## M7-A implementation evidence

- `load_stage1_package` inspects archive safety/CRC, requires the manifest as
  the first member, reads members without extraction, and reuses the Stage 1
  story, report, character-reference, manifest and optional-anchor validators.
- The intake returns immutable bytes plus parsed views bound to the SHA-256 of
  the exact source ZIP and verifies that the source bytes did not change.
- `visual_plan.json` enforces the canonical 10-field root, 15-field asset
  records, selection coverage shape, digest projection and exact ordered ZONE
  basename set.
- `visual_bible.json` now uses the canonical schema version 2.0 and exact
  13-field root; story/dependency digests, profile, art-direction and ZONE map
  shape are fail-closed.
- Targeted Stage 2 and Stage 1-intake tests: 31 passed.
- Full baseline: Ruff format PASS; Ruff lint PASS; mypy PASS; pytest 305 passed,
  2 skipped; total coverage 92.70%; `git diff --check` PASS.
- Canonical SHA-256 remains
  `4c021a2e61df611a566c83c22fdf4378617314147de8dd6eaa9693160205ce27` and
  the file remains read-only.

## M7-B implementation evidence

- Stage 2 planning derives `visual_plan.json` and `visual_bible.json` only from
  the validated immutable Stage 1 package view and binds exact story, report,
  scene-map, character-reference and profile digests.
- The package basename order and execution queue are frozen to the canonical
  ten-name ZONE sequence: `introduction.png` identity pilot,
  `opening.png` calibration, `cover.png`, Wave B and Wave C.
- Visual Bible planning materializes profile-bound identity locks, character
  reference hashes, preliminary wardrobe state for all ten basenames,
  art-direction/tonal locks and a recomputable dependency digest.
- The basename-scoped payload compiler exposes only the current target,
  current scene objective, applicable identity/wardrobe locks, text policy and
  negative constraints. It never passes the full story, manifest, queue,
  progress or checkpoint context to the image backend.
- The invocation firewall deterministically checks Stage 2/LANDSCAPE,
  `FINAL_REQUIRED_ASSET`, exact 3840x2160 dimensions, one output, queue order,
  target/reference scope, forbidden global keys and foreign future basenames.
- Tests compile all ten invocations without calling an image adapter and prove
  fail-closed behavior for cardinality and out-of-order targets.
- Full baseline after M7-B: Ruff format/lint PASS; mypy PASS; pytest 305 passed,
  2 skipped; total coverage 92.87%; `git diff --check` PASS.

## M7-C1 transaction and progress evidence

- The frozen ZONE queue is connected to the M6 single-image transaction kernel
  through a mock-safe executor. Every call uses one `LANDSCAPE` transaction,
  one basename, one 3840x2160 canvas and one output.
- Transaction orientation is now persisted as the artifact orientation
  (`LANDSCAPE`) instead of the owner-stage label, matching the canonical model.
- `stage_image_progress.json` schema 1.0 is implemented with its exact 21-field
  root and nine-field asset records. Counts, indexes, paths, source, hashes,
  transaction IDs, missing assets, next basename and self-digest are derived
  from committed database bindings and validated after atomic write/reopen.
- Exposed progress normalizes retryable failures to PENDING and contains no
  candidate bytes or failed count. When all ten assets are committed the resume
  record is no longer exposed; execution stops at `READY_FOR_AGGREGATE_GATES`.
- The integration fixture injects one failure at `development.png`: the first
  run commits only pilot, calibration and cover; continuation retries only the
  same logical transaction, succeeds at attempt 2, and completes ten unique
  committed basenames without duplicate bindings.
- No ComfyUI or production image call was made. Full baseline: Ruff
  format/lint PASS; mypy PASS; pytest 306 passed, 2 skipped; coverage 92.98%;
  canonical hash/read-only and `git diff --check` PASS.

## M7-C2a gate evidence

- Exact committed PNG bytes are reopened and checked for the ten-name set,
  unique transaction cardinality, dimensions, stored digest and role-specific
  pilot/calibration/cover/outro/wave gates.
- Missing or non-observable semantic assessment remains `NOT_VERIFIED` and
  blocks aggregate release. Ten independent test-only assessment records with
  observable findings then pass semantic truth and aggregate coherence.
- This is fixture evidence only; uniform mock pixels are not claimed valid for
  production. Targeted test passed in 53.62 seconds.

## M7-C2b commitment evidence

- Added a pre-commit binder for `image_provenance_commitment` and
  `visual_realization_commitment` on every new Stage 2 PNG. The realization
  record is schema 2.0, `CREATION_BOUND`, `FINAL/PASS`, and binds the current
  plan snapshot, transaction role/index, semantic-claim digest, evidence and
  decoded-pixel digest.
- PNG `iTXt` parsing and CRC validation are implemented; every committed mock
  asset is reopened and checked for basename, orientation, transaction,
  provenance stage and pixel digest.
- Full baseline after C2b: Ruff format/lint PASS; mypy PASS; pytest 306 passed,
  2 skipped; coverage 92.93%; canonical hash/read-only and `git diff --check`
  PASS. No ComfyUI production call was made.

## PCF001 disposition

`PCF001_STAGE4_AUDIO_MODE_DEFAULT_COLLISION` remains an intentional canonical
finding. It concerns Stage 4 audio defaults, is outside M7 Stage 2 ZONE, and
cannot be silently resolved while canonical v3.16.13 is read-only. Existing
tests must continue to report it.

## M7-D checkpoint package evidence

- Added gate-first `build_stage2_checkpoint`; publication requires aggregate
  PASS and ten independently assessed semantic PASS records.
- The package preserves Stage 1 story/report/character/anchor members as
  read-only, then adds exact visual plan, visual bible and ten committed
  landscape PNGs.
- Manifest parent digest, ownership, mutation status, canonical file order,
  extraction safety and exact bytes are checked before returning the package
  digest. ComfyUI production acceptance remains separate.

## Next checkpoint

Next checkpoint: M7-E should verify the exported ComfyUI API workflow JSON
against the local installation and checkpoint path, then perform one
explicitly authorized local smoke call without network or model downloads.

## M7-E preflight and smoke evidence

- `comfy_workflows/sdxl_txt2img_api.json` parses as valid API JSON.
- ComfyUI Desktop is installed and the local API at `127.0.0.1:8188` responds;
  reported version is 0.35.1 with the RTX A4500 Laptop GPU available.
- The requested checkpoint path
  `D:\Documents\ComfyUI\models\checkpoints\sd\_xl\_base\_1.0.safetensors`
  is absent, and no matching checkpoint was found under the configured local
  ComfyUI model roots. No download or cloud fallback was attempted.
- The checkpoint is now present at the requested path and ComfyUI lists the
  exact filename `sd_xl_base_1.0.safetensors`.
- The exported API workflow was queued locally with prompt id
  `5f74ab5e-0eab-4f97-9100-fb41fc15db99`; ComfyUI returned `success` with one
  output and no node errors.
- Output was written to
  `D:\Documents\ComfyUI\output\audio_story_sdxl_00002_.png` (1,525,311
  bytes). This was a local smoke test only; it is not published as a Stage 2
  landscape because it is 1024x1024 and lacks Stage 2 commitments.
