# Milestone M5 Status

## Outcome

M5 implements the Stage 1 deterministic-mock vertical slice, satisfies the reviewed M5
acceptance matrix, and remains uncommitted for final review.
It does not implement M6, call ComfyUI/OCR, alter canonical bytes, use cloud services or resolve
`PCF001_STAGE4_AUDIO_MODE_DEFAULT_COLLISION`.

## Architecture

- Typed three-profile router and explicit duration-confirmation `WAITING_INPUT`/resume path.
- M1 Stage 1 capsule binding; no full canonical prompt is placed in generation instructions.
- Separate M4 calls for plan, draft, review, repair and serialize with M3 transaction/call lineage.
- Strict Stage 1 story/report/manifest validation built on the M2 strict parser and digest helpers.
- Deterministic, test-only character references with explicit provenance; production stops at
  `WAITING_DEPENDENCY`.
- Deterministic Stage 1 ZIP metadata/order, safe extraction, exact member reopen and M3 artifact
  commit only after post-package PASS.

## Verification

Verification from the final working tree:

- `ruff format --check .`: PASS (`80 files already formatted`).
- `ruff check .`: PASS.
- `mypy src`: PASS (`41 source files`).
- `pytest`: PASS (`176 passed, 1 skipped`).
- Repository branch coverage: `94.60%`.
- M5 branch coverage: domain router `97%`, validator/serializer `100%`, workflow `95%`,
  packager `95%`, and CLI integration surface `100%`; every M5 core module exceeds `85%`.
- `git diff --check`: PASS.
- The one SKIP is the explicit local llama.cpp smoke test because no backend is configured; this
  is not a blocker for the deterministic default suite.
- Three-profile integration, fresh-instance recovery matrix, retry-lineage, retry-exhaustion, duration resume, production mock
  firewall, strict JSON negative cases, package reopen, and two-independent-run byte
  repeatability: PASS.

## Limits

- Mock character PNGs prove binding, packaging and repeatability only; production visual quality
  belongs to M6.
- Semantic quality evidence in the deterministic integration fixture is explicitly mock evidence,
  not independent production assessment.
- SERIAL_DETECTIVE materializes schema 3.2.0 `series_anchor.json`; non-serial packages reject it.
- Thirteen fresh-instance crash boundaries plus all five adapter-phase retry boundaries verify
  database/filesystem state, event log, lineage, progress, idempotency and package publication.
- Serialized-audio dialogue tests cover gender mismatch, narrator misuse, reported speech and
  ambiguous/valid same-voice exchanges without using Beat Map evidence.
- PCF001 remains unresolved.
