# M31 Status — STAGE progress detail UI

- Added a live STAGE detail panel to `ui/dist/index.html`.
- Projection reads only the existing `/api/v1/studio` snapshot: stage progress,
  transactions, latest generation call, committed artifact digest, gates, and
  `updated_at`.
- Deterministic progress remains `committed / total`; no ETA is guessed and no
  package/schema layout is changed.
- Browser JavaScript syntax check: PASS (`node --check ui/dist/app.js`).
- `git diff --check`: PASS.
