# Milestone M0 Status

## Outcome

Milestone M0 is complete. The repository has an installable Python baseline, an offline local environment diagnostic, a preserved canonical prompt baseline, test tooling, and the target RTX A4500 configuration.

## Completed work

- Initialized the Git repository without creating a commit.
- Preserved `ChatGPT_prompt_v3.16.13.txt` byte-for-byte under `canonical/`, marked it read-only, and recorded SHA-256 `4c021a2e61df611a566c83c22fdf4378617314147de8dd6eaa9693160205ce27`.
- Added repository invariants in `AGENTS.md`, including offline-only behavior, deterministic validation ownership, v3.16.13 compatibility, loopback binding, and one GPU-heavy job at a time.
- Added an editable Python package, development dependencies, Ruff, mypy, pytest, branch coverage, and pre-commit configuration.
- Added `audio-story doctor`. It emits a single JSON document and performs local inspection only for Python, CUDA, NVIDIA GPU and VRAM, RAM, disk, llama.cpp, ComfyUI, and PaddleOCR availability.
- Added safe defaults for the RTX A4500 Laptop 16 GB target: `max_parallel_gpu_jobs=1`; local service URLs bind to `127.0.0.1`.
- Added unit tests and placeholder directories for the planned repository layers. No M1 compiler behavior was implemented.

## Files changed

- `AGENTS.md`, `README.md`, `.env.example`, `.gitignore`, `.pre-commit-config.yaml`
- `pyproject.toml`
- `canonical/ChatGPT_prompt_v3.16.13.txt`, `canonical/prompt.sha256`
- `src/audio_story/__init__.py`, `src/audio_story/cli.py`, `src/audio_story/config.py`, `src/audio_story/doctor.py`
- repository-layer placeholders under `src/audio_story/`
- `tests/unit/test_cli.py`, `tests/unit/test_config.py`, `tests/unit/test_doctor.py`
- test, schema, workflow, migration, script, ADR, and status directory placeholders

## Verification

Executed with the bundled Python 3.12.14 runtime:

```text
python -m pip install -e ".[dev]"     PASS
python -m ruff format --check .       PASS
python -m ruff check .                PASS
python -m mypy src                    PASS
python -m pytest                      PASS: 7 tests, 96.25% branch coverage
python -m audio_story.cli doctor      PASS: valid JSON
Get-FileHash -Algorithm SHA256 canonical/ChatGPT_prompt_v3.16.13.txt
                                      PASS: 4C021A2E...05CE27
```

The default tests use mocks for executable/GPU discovery and make no network calls.

## Target inventory observed

The local diagnostic observed Python 3.12.14, CUDA 13.2 tooling, NVIDIA RTX A4500 Laptop GPU with 16,384 MiB, NVIDIA driver 596.52, approximately 128 GB RAM, and local disk capacity. llama.cpp, ComfyUI, and PaddleOCR executables were not found on `PATH`; this is expected at M0 and is reported without network access or automatic download.

## Remaining issues and risks

- No model/backend is provisioned at M0. Local backend installation and hardware smoke tests belong to later milestones.
- The plan identifies a canonical Stage 4 audio-default collision that must be reported by M1 and decided before M9; M0 does not resolve or mutate it.
- The development install retrieved Python build/test packages. This does not affect runtime offline behavior, but a wheelhouse/SBOM and fully air-gapped installer are explicitly deferred to M11.
- The repository currently has no initial commit; review and commit are left to the user.

## M0 Definition of Done

- Repository installs with one command: PASS.
- `pytest` succeeds: PASS.
- `doctor` emits machine-readable JSON: PASS.
- Default tests perform no network calls: PASS.

Recommendation: M0 is ready for review and can proceed to M1 after the baseline is committed. The known Stage 4 ambiguity does not block M1 parsing work, but M1 must detect and report it rather than silently choose a value.
