# Milestone M8 Status

Status: COMPLETE.

## Scope

M8 is Stage 3 portrait generation and final-package publication. The reviewed
implementation boundary and Definition of Done are recorded in
`docs/m8-stage3-portrait-package-plan.md`.

## Entry evidence

- M7 status: COMPLETE.
- Authoritative Stage 2 checkpoint SHA-256:
  `47db5f8ae31364caafea37019d38f0aa8f7823936c3fc1bbac998f55e1ff4cd2`.
- Canonical prompt remains byte-for-byte frozen and read-only.
- Local-only, loopback-only and one-GPU-heavy-job invariants remain active.

## M8-A completion evidence

- Added a read-only Stage 2 package intake that checks ZIP safety/CRC, the
  authoritative archive SHA-256, manifest field order and package digest,
  member order/hash/size/owner/mutation status, story/report/character
  bindings, Visual Plan/Visual Bible bindings and all ten landscape PNG
  commitments.
- Reopened `artifacts/m7-production-d26/story.zip` successfully at exact
  SHA-256
  `47db5f8ae31364caafea37019d38f0aa8f7823936c3fc1bbac998f55e1ff4cd2`.
  Its recomputed manifest package digest is
  `63ffff0515e3f72c8762b66bf5894ec5d30c947df4e37300c9d4fcbc134cc75f`.
- Froze 15 inherited member authority records with exact owner stage, byte
  length and SHA-256. The mutation guard rejects missing, extra or one-byte
  changed inherited members.
- Froze the ten portrait paths in canonical ZONE basename order and the
  27-member Stage 3 final file set for this one-character, no-anchor package.
- Registered and implemented `package_quality_report.json` schema 2.0 with
  exact 13-field root order, canonical nested identity/registry/summary/
  dimension/component/image/blocker/validation shapes, twelve ordered quality
  dimensions, deterministic serialization, deterministic report ID and
  parse-back self-digest validation.
- Targeted M8-A/schema tests: 12 passed.
- Full suite: 327 passed, 2 skipped; branch coverage 89.03% (threshold 85%).
- `ruff format --check .`, `ruff check .`, strict `mypy src`, canonical prompt
  SHA-256 and `git diff --check` pass.
- No image-generation call was made during M8-A.

## Next checkpoint

M8 is closed. Do not begin a later milestone without an explicit milestone plan.

## M8-B completion evidence

- Materialized one deterministic portrait adaptation record for every canonical
  ZONE basename. Each record includes the required semantic moment, subject,
  crop/reposition, headroom, body-crop, text-safe-area, lens, background,
  exposure and identity decisions and binds the exact matching landscape hash
  plus minimal character-reference set.
- Applied the canonical portrait pilot risk weights. For the authoritative M7
  package, `introduction.png` and `cover.png` both score 5; the mandatory active
  cover-typography tie-break selects `cover.png`. The pilot evidence and
  validation digest are deterministic, and the execution queue contains the
  pilot once followed by the other nine basenames in package order.
- Added an abort-before-call Stage 3 firewall requiring `STAGE3`, `PORTRAIT`,
  `FINAL_REQUIRED_ASSET`, one semantic target, one output, one 1080x1920 canvas,
  the current basename only and exact references from the inherited-byte
  authority map. Package/batch/storyboard/future-basename context is rejected.
- Added strict Stage 3 progress serialization and parse-back validation for the
  21-field CURRENT contract, portrait execution/package indices, queue states,
  counts and self-digest.
- Targeted M8 tests: 3 passed, including wrong queue order, dimensions,
  cardinality, global/batch context and duplicate progress basename failures.
- Full suite: 328 passed, 2 skipped; branch coverage 89.18% (threshold 85%).
- `ruff format --check .`, `ruff check .` and strict `mypy src` pass.
- No image-generation call was made during M8-B.

Before any M8-C call, reopen the authoritative Stage 2 package, recompute the
adaptation/pilot evidence and run the exact pilot invocation firewall. Execute
only `portrait/cover.png` first; remaining portraits stay blocked until the
pilot's full identity, crop, anatomy, luma, typography, provenance,
cross-orientation and postwrite gates pass.

## M8-C implementation evidence

- Added CURRENT Stage 3 portrait provenance and visual-realization commitments
  with exact transaction, 1080x1920 final bytes, art direction and matching
  authoritative landscape basename/hash bindings.
- Added the pilot-first executor on the shared one-GPU single-image transaction
  kernel. It creates one logical transaction per basename, retries only the
  failed basename within the two-attempt budget and never counts a failed
  attempt or regenerates a committed pilot.
- The deterministic mock execution committed all ten portraits. An injected
  first-attempt failure on `opening.png` produced two calls for that basename,
  one transaction and one authoritative commit; `cover.png` remained exactly
  one pilot transaction.
- Targeted Stage 3 tests: 4 passed. Full suite: 329 passed, 2 skipped; branch
  coverage 89.17% (threshold 85%). Ruff format/lint and strict mypy pass.
- Production preflight recomputed the M7 package/plan/firewall successfully.
  The installed runtime was resolved at
  `D:/Comfy-Desktop/ComfyUI-Installs/ComfyUI/ComfyUI` and started directly with
  custom/API/Manager nodes disabled on `127.0.0.1:8188`; health reports ComfyUI
  0.35.1 and the RTX A4500 Laptop GPU. The executor now requires a callable
  exact-byte semantic assessor, so it cannot silently commit pixels without
  independent evidence. That production assessor is not yet wired for Stage 3;
  therefore no pilot image call was made. No cloud fallback or download was
  attempted. M8-C is not production-complete until `cover.png` passes the
  independent pixel/semantic, typography, parity and postwrite gates.

## Historical M8-C next action (resolved)

Wire the provisioned offline Qwen2.5-VL assessor into the Stage 3 executor,
export or verify a 1080x1920 portrait API workflow, then execute only the
`cover.png` pilot. Keep the remaining nine-item queue closed until all pilot
gates pass.

## Latest M8-C preflight

- ComfyUI 0.35.1 is currently healthy on `127.0.0.1:8188` with the RTX A4500
  Laptop GPU and the provisioned local SDXL checkpoint path.
- The available ComfyUI API workflow is still a 1024x1024 landscape workflow;
  it is not authorized for a Stage 3 portrait call.
- A local Qwen2.5-VL-7B-Instruct bundle is present at
  `D:/Documents/Models/Qwen2.5-VL-7B-Instruct`; `AutoProcessor` resolves to
  `Qwen2_5_VLProcessor` with `local_files_only=True`. Full GPU model-load and
  assessor execution remain a separate preflight and have not been run against
  a portrait image. The executor therefore remains fail-closed before calling
  the image backend.
- Full quantized GPU-load preflight now passes on `cuda:0` / RTX A4500 with
  `local_files_only=True`; no image generation or ComfyUI queue submission was
  performed.
- Production image-call count remains zero. The next execution may start only
  after both the portrait 1080x1920 workflow and offline vision assessor pass
  their local-only preflight.

## Follow-up execution checkpoint

- Added `comfy_workflows/sdxl_txt2img_portrait_api.json`, derived from the
  existing SDXL API graph with an explicit `1080x1920`, `batch_size=1` latent
  and Stage 3 filename prefix. JSON parsing and dimension assertions pass.
- This prepares the workflow side of the preflight; it does not authorize an
  image call. The offline Qwen2.5-VL assessor remains the outstanding gate.

## M8-C production execution evidence

- Added the offline Qwen2.5-VL callable adapter and the Stage 3 production
  runner with preflight, single-pilot and resume-safe `--execute-all` modes.
- Corrected the Stage 3 `ImageRequest` construction to bind the prompt digest
  and workflow-file digest to their named fields. The prior positional call
  inverted those authorities and failed closed with `IMG013_WORKFLOW_DIGEST`.
- The authoritative M7 package archive loaded successfully. The `cover.png`
  pilot committed first at 1080x1920 after exact-byte semantic assessment.
- Resume-safe execution then committed all remaining portraits sequentially on
  the same Stage 3 run. Final runtime state is 10/10 committed,
  `next_pending_basename=null`, and `READY_FOR_AGGREGATE_GATES`.
- Aggregate runtime audit confirms every canonical portrait basename has
  status `COMMITTED` and exactly one `COMMITTED` artifact binding. No Stage 3
  aggregate-gate/package builder exists yet; schema 2.0 serialization and
  validation are implemented, but production report assembly and checkpoint
  archive emission remain outstanding.

The final sentence above records the earlier M8-C checkpoint; M8-D resolved
that outstanding work.

## M8-D completion evidence

- Added deterministic twelve-dimension package-quality aggregation with score,
  verdict and blocker reconciliation in code. Invalid recomputed scores fail
  closed with `M8A109_SCORE_RECOMPUTE`.
- Added exact-byte Stage 3 package assembly, canonical FILE-SET-01 ordering,
  immutable inherited-member enforcement, portrait commitment checks, stable
  ZIP metadata, safe extraction, CRC, parse-back, parent binding and postwrite
  verification.
- Added idempotent lifecycle publication. Repeating the same publication
  returns the existing package; the production runtime contains one package
  transaction and one `IMAGE_PACKAGE_PUBLISHED` event.
- Published the explicit offline production output at
  `artifacts/m8-stage3-runtime3/final/story.zip`: 145,532,863 bytes, 27 members,
  archive SHA-256
  `85e941ce77cd61826f30e06098118e3c2be29a3952b01f806e96ca82f8eb462e`,
  package digest
  `7ecd8f0428b75ffe6d17fe02530b73fa16dd3d2185fb7580c7efc1b0cfe133c4`,
  and parent package digest
  `63ffff0515e3f72c8762b66bf5894ec5d30c947df4e37300c9d4fcbc134cc75f`.
- Production audit: ten distinct committed portrait transactions; all portrait
  PNGs are 1080x1920; report verdict `PASS`, overall score 100, twelve ordered
  dimensions; ZIP `testzip()` returned no failing member. Stage status is
  `PASS` and workflow status is `COMPLETED`.
- Baseline: `ruff format --check .`, `ruff check .`, strict `mypy src`, and
  full `pytest` pass: 329 passed, 2 skipped, branch coverage 89.10% (threshold
  85%). Canonical SHA-256 remains
  `4c021a2e61df611a566c83c22fdf4378617314147de8dd6eaa9693160205ce27`.
