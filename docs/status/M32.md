# M32 Status — welcome workspace picker

## Stage 1 quoted-sentence retry recovery

- Diagnosed a production failure at `segment-opening-004-01.json`: the local
  model returned a complete Vietnamese question ending in a closing quote
  (`…?'`), but the deterministic terminal-punctuation gate inspected only the
  final character. It incorrectly rejected all four attempts as
  `S162_ZONE_GENERATION_FAILURE`, exhausting the local retry budget.
- The sentence gate now deterministically ignores only supported trailing
  closing quotes and brackets before testing for a sentence terminator. A
  closer without a preceding terminator remains invalid. This changes neither
  the canonical prompt nor the `story.zip` layout.
- Verification: 79 focused Stage 1 unit/integration tests pass with
  `--no-cov`; targeted Ruff format/check and strict mypy pass. The canonical
  prompt SHA-256 and Git whitespace checks pass.

## Stage 1 concrete-focus recovery

- Diagnosed the subsequent `segment-opening-001-01.json` failure. Although
  the retry path varied seed and wording, the model had already generated 30
  very short GREETING fragments that repeatedly paraphrased the same “promise
  repair shop” premise. The OPENING candidates then repeated those fragments
  and correctly hit `S166_SEGMENT_DUPLICATE`.
- Every bounded segment now receives a deterministic, position-specific
  concrete focus: an object coupled to an action, question, decision,
  consequence, interaction, obstacle, or discovery. It must be used as the
  text's main action, not scenery. This supplies local generation with a
  progressing story move without weakening the duplicate gate or changing the
  canonical prompt.
- Verification: 62 focused Stage 1 unit/integration tests pass with
  `--no-cov`; Ruff check/format and strict mypy pass. The `story.zip` layout
  was not changed.

## Stage 1 anti-copy duplicate recovery

- Diagnosed the `segment-opening-003-02.json` failure after 40 committed
  segments and 55 retries. Its four candidates were short variations of
  already committed “enter the room / familiar scent / warm light” sentences.
  The deterministic duplicate detector was correctly rejecting them.
- Root cause: the generation prompt embedded the full text of every committed
  segment while instructing the local model not to copy it. That supplied a
  growing set of next-token examples for the model to paraphrase, especially
  after long runs. Prompts now expose only the committed-segment count; the
  full corpus remains in code for deterministic duplicate comparison. A
  duplicate retry uses an action–object/person/clue–consequence sentence shape
  and does not quote the duplicated sentence back to the model.
- Verification: 62 focused Stage 1 unit/integration tests pass with
  `--no-cov`; Ruff check/format and strict mypy pass. The canonical prompt and
  `story.zip` layout were not changed.

## Stage 1 repeated-plan recovery

- Diagnosed the next failed Studio transaction `segment-greeting-003-01.json`.
  The authoritative local `plan.json` contained one “old house/memories” beat.
  The story-bible projection duplicated it across all eight zones, leading the
  local writer to produce and correctly fail near-identical arrival/memory
  segments under `S166_SEGMENT_DUPLICATE`.
- A supplied plan beat now applies only to its matching canonical zone; zones
  without a supplied beat receive their canonical progression role rather than
  a duplicated event. Duplicate retry feedback also requires a concrete new
  action, decision, discovery, or interaction and explicitly rejects mere
  house/memory/wind/leaves descriptions as the main event. The duplicate gate
  remains unchanged.
- Verification: 62 focused Stage 1 unit/integration tests pass with
  `--no-cov`; Ruff check/format and strict mypy pass. The canonical prompt and
  `story.zip` layout were not changed.

## Stage 1 local segment retry recovery

- Diagnosed the failed Studio transaction `segment-greeting-005-03.json`:
  two Vietnamese candidates duplicated an earlier autumn-leaves scene, then
  the local model returned Chinese prose without terminal punctuation and a
  final Chinese candidate over the segment word ceiling. Language validation
  previously ran only after the whole story was assembled, so it could not
  correct an individual candidate.
- Each candidate is now language-gated before it can commit; wrong-script or
  wrong-language prose is retained and recorded as `S167_LANGUAGE_MISMATCH`
  and receives a selected-language retry correction. Final hard constraints
  repeat the selected language and exact segment word range after all long
  context. Position- and retry-specific novelty cues now genuinely differ,
  rather than silently discarding their indices.
- Verification: 61 focused Stage 1 unit/integration tests pass with
  `--no-cov`; Ruff check/format and strict mypy pass for touched files. The
  canonical prompt and `story.zip` layout were not changed.

## Stage 1 duplicate-segment recovery

- Diagnosed the failed Studio transaction `segment-greeting-003-03.json`.
  Its four candidates repeated variations of the same “Nam meets a lost dog”
  event, each exactly matching an earlier committed segment and therefore
  correctly rejected as `S166_SEGMENT_DUPLICATE` before the retry budget was
  exhausted.
- Each segment now has a deterministic, position-specific novelty cue. A
  duplicate retry advances to a different cue, so it changes the requested
  story event rather than only changing seed and temperature. The existing
  duplicate gate, candidate retention, and four-attempt bound remain intact.
- Regression coverage verifies cues differ between segments and duplicate
  retries. Focused Stage 1 tests, Ruff, and strict mypy pass; no canonical
  prompt or package-layout change was made.

## Stage 1 selected-language enforcement

- The Studio language dropdown already propagated `vi` or `en` into package
  metadata and each script item's `lang` field, but generation instructions
  did not make that selection an explicit constraint. A local model could
  therefore return Chinese prose while the package was labelled Vietnamese.
- Every production phase and every bounded zone segment now receives an
  explicit selected-language instruction. Before `serialize.json` commits,
  deterministic validation rejects CJK, Japanese, or Korean script for both
  supported selections; Vietnamese requires a proportional presence of
  Vietnamese diacritics and English rejects them. The stable rejection is
  `S167_LANGUAGE_MISMATCH`, so a mismatched package cannot be published.
- Verification: Vietnamese and English acceptance, Chinese rejection, and
  Vietnamese-for-English rejection are covered by regression tests. Focused
  Stage 1 unit/integration tests, Ruff, and strict mypy pass. The canonical
  prompt and `story.zip` layout were not changed.

## Stage 1 Unicode terminal punctuation recovery

- Diagnosed the failed local Studio transaction
  `segment-greeting-001-07.json`: after an initial duplicate, three retained
  candidates ended in Chinese `。` or `？`. The deterministic sentence gate
  accepted only `.`, `!`, and `?`, so the valid complete sentences exhausted
  the bounded local retry budget as `S162_ZONE_GENERATION_FAILURE`.
- Stage 1 zone, serialized-story, and production-script validation now accept
  the corresponding CJK terminal punctuation (`。`, `！`, `？`) in addition to
  the existing ASCII marks. Retry feedback states those accepted marks, and
  outline aggregation retains the original terminal mark rather than adding a
  spurious period.
- Verification: both saved failed candidates now validate with their original
  one-item contract; 40 focused Stage 1 unit/integration tests pass; Ruff
  format/check, strict mypy, and `git diff --check` pass. The canonical prompt
  and `story.zip` layout were not changed.

## Stage 1 CJK word-budget recovery

- The next local Studio run committed all eight generated segments for
  `GREETING` item 001, but then failed with `S163_ZONE_RETRY_EXHAUSTED`:
  `item word budget exhausted`. The prior generic `\w+` counter treated each
  uninterrupted Chinese clause as one or two words, yielding 13 instead of
  the 154 CJK ideographs in those retained candidates.
- The deterministic counter now counts CJK ideographs individually while
  preserving whitespace-delimited word counting for Latin-script prose. Zone
  gates, per-item completion, retry diagnostics, and final duration all use
  the same counter, preventing their budgets from disagreeing.
- Verification: the retained eight candidates count as 154; 41 focused Stage
  1 unit/integration tests pass; Ruff format/check, strict mypy, and `git
  diff --check` pass. The canonical prompt and `story.zip` layout were not
  changed.

## Stage 01 creation-dialog alignment

- Reworked the Studio Stage 01 dialog into a two-step local flow: one explicit
  content profile, one content source (preset, topic, brief, plot idea, or
  random), profile-specific controls, then duration confirmation.
- Duration recommendations now vary by youth age band and serialized-detective
  episode mode; direct custom values are bounded by the displayed canonical
  interval before submission. A continuation/finale request requires a local
  `series_anchor.json` selection in the UI.
- The optional title and source input are passed through the local command
  bridge as bounded creative context for production structured calls. Seed is
  optional and generated locally when absent. No prompt, archive layout,
  cloud fallback, or external service was added.
- Verification: `node --check ui/dist/app.js`; focused Stage 1/Studio tests:
  41 passed with `--no-cov`; Ruff format/check and strict mypy pass for the
  touched Python modules. The canonical SHA-256 remains
  `4c021a2e61df611a566c83c22fdf4378617314147de8dd6eaa9693160205ce27`.

## Stage 1 canonical authoring normalization

- Production zone payloads remain an internal schema and are deterministically
  aggregated before `serialize.json`: `item_id` and `speaker_id` are excluded;
  final script items use exactly `zone, environment, voice, speed, lang, text`.
- The aggregation produces all eight canonical outline keys in order and maps
  the internal narrator representation to `environment=none`, `voice=NARRATOR`,
  `speed=NORMAL`, and `lang=uppercase(meta.language)`.
- Regression coverage verifies both the normalization boundary and the committed
  `serialize.json` artifact. Focused result: 39 passed; Ruff format/check PASS.
- Canonical SHA-256 remains
  `4c021a2e61df611a566c83c22fdf4378617314147de8dd6eaa9693160205ce27`.

- Added an offline-first welcome screen shown when the Studio UI opens.
- Added actions for a new empty workspace, selecting an existing workspace
  directory, and selecting an existing `story.zip` file.
- Empty snapshots now reset the breadcrumb, title, counts, and metadata, and
  hide story-only progress cards so no demo story information remains visible.
- Browser selection is surfaced locally with a toast; no cloud fallback,
  credentials, model download, schema, or `story.zip` layout changes were made.
- JavaScript syntax check: PASS (`node --check ui/dist/app.js`).
- `git diff --check`: PASS (including the nested `ui` checkout).

## Known integration boundary

The current server process receives one `--workspace` path at startup and
does not yet expose a safe runtime workspace-switch API. The new picker keeps
the selected local path visible to the user, while true server-side reopening
requires that separate API/command bridge.

## Stage 1 Studio local-model routing correction

- Root cause: the Studio command bridge constructed `DeterministicMockAdapter`
  directly and marked every Stage 1 request `test_mode=True`; model responses
  therefore could not become story content.
- `StudioCommandRunner` now accepts an injected Stage 1 adapter and execution
  mode. Unit-test callers retain the explicit deterministic fixture path, while
  `serve_studio` binds the production path to the loopback-only llama.cpp
  adapter with `test_mode=False`.
- The Studio CLI accepts explicit local llama endpoint, executable, and model
  paths. `open_app.bat` supplies the already provisioned, hash-reviewed local
  Qwen2.5-7B GGUF and llama.cpp executable. The server is started locally,
  health-checked, and terminated with Studio; no download or cloud fallback was
  added.
- Verification: 392 focused/repository tests pass with 2 skipped; the existing
  repository-wide coverage gate remains independently below its configured 85%
  threshold (83.45%). Ruff and strict mypy pass. The canonical prompt digest
  remains `4c021a2e61df611a566c83c22fdf4378617314147de8dd6eaa9693160205ce27`.

## All-stage production model routing correction

- Stage 2 now defaults to `COMFYUI`; the Studio UI no longer offers the
  deterministic mock mode. Mock remains available only as an explicit test API
  fixture. Production image generation uses the configured loopback ComfyUI
  adapter and semantic review uses the local Qwen2.5-VL assessor.
- Stage 3 no longer constructs `DeterministicMockImageAdapter` while reporting
  `VLM_LOCAL`. Its production route now generates portrait assets through local
  ComfyUI and reviews them with the local Qwen2.5-VL assessor. Production input
  validation also no longer enables Stage 2 test-package acceptance.
- Stage 1 is wired end-to-end to local llama.cpp for writing and story-quality
  assessment, local ComfyUI for character references, and local Qwen2.5-VL for
  character authority. The Studio launcher starts the provisioned local
  llama.cpp and ComfyUI processes and terminates both on shutdown.
- Stage 4 keeps deterministic schema/timeline arithmetic in code, but now sends
  the completed video-prompt package to the local llama.cpp model for semantic
  fidelity and continuity review before packaging and FFmpeg rendering. A model
  `FAIL` blocks publication.
- Focused Studio verification passes (21 tests); Ruff and strict mypy pass.

## Studio launcher readiness correction

- Root cause of the browser `ERR_CONNECTION_REFUSED`: `open_app.bat` opened the
  browser after a fixed one-second delay, while local llama.cpp and ComfyUI
  startup and health checks can take roughly one minute before Studio binds
  `127.0.0.1:4173`.
- The launcher now starts a hidden, bounded readiness helper that polls
  `/api/v1/health` and opens the browser only after HTTP 200. It times out after
  180 seconds instead of opening a known-unready URL.
- Live verification passed: Studio, llama.cpp, and ComfyUI listened only on
  `127.0.0.1`; `/api/v1/health` returned
  `{"status":"PASS","offline":true,...}`.

## Workspace/story opening semantics

- Removed the misleading directory picker that only dismissed the welcome
  screen without changing the server workspace. Because the local HTTP server
  binds its workspace at process startup, the welcome screen now identifies
  the launcher-selected current workspace and directs other-workspace opening
  through `open_app.bat <workspace-path>`.
- Replaced the story filename-only picker with a real ZIP import endpoint.
  Browser bytes are accepted only as ZIP/octet-stream with a 512 MiB bound,
  parsed for `workflow_manifest.json`, validated by the existing Stage 1/2/3
  intake validator, content-addressed under `imports/<sha256>/story.zip`, and
  registered as the latest PASS package for the corresponding stage.
- Invalid ZIPs, unsupported stages, malformed manifests, validation failures,
  empty bodies, and conflicting content fail closed with stable UI error codes.
- Focused Studio verification passes (23 tests); Ruff and strict mypy pass.

## Stage 1 bounded-generation failure correction

- Stage 1 segment retries now aim at the exact midpoint of the deterministic
  word budget and permit one additional bounded attempt. This prevents the
  observed oscillation from 32 words (`S165_SEGMENT_OVER_BUDGET`) to 19 words
  (`S164_SEGMENT_UNDER_BUDGET`) around the required 20–30 word range.
- A terminal `Stage1Error` now transitions the active stage and workflow to
  `FAIL` and `FAILED`. The Studio job also retains the failed workflow/stage
  identifiers, and the queue view displays the stable error code and message.
- Focused Stage 1 and Studio command verification: 48 tests pass. Ruff checks,
  Ruff format checks, and the UI JavaScript syntax check pass.

## All-stage execution observability

- Replaced the queue placeholder with a shared Stage 1–4 execution dashboard:
  deterministic transaction progress, active step, attempt, elapsed time,
  runtime identity, workflow/stage/job identifiers, filters, live events, and
  terminal result metrics.
- Transaction rows open a lineage inspector with all persisted generation-call
  attempts, failure codes, termination reasons, committed artifact digest/size,
  and current gate results. Failed jobs display their stable code and message.
- The SQLite snapshot now projects ordered call history plus request/response
  digests for every transaction. Stage selectors open the matching execution
  view without introducing another source of authority.
- No schema, canonical prompt, cloud behavior, or `story.zip` layout changed.

## Stage 1 adaptive segment retry correction

- The observed production failure oscillated from an over-budget response to a
  19-word response while the deterministic gate requires 20–30 words. Retry
  feedback now includes the rejected text's measured Unicode word count and the
  exact number of words to add or remove to reach the 25-word midpoint.
- Counting remains a deterministic code gate, retries remain bounded to three,
  and rejected model bytes remain preserved as candidate artifacts. No schema,
  package layout, canonical prompt, cloud fallback, or automatic download was
  introduced.
- Verification: 396 tests passed with 2 skipped; Ruff checks and strict mypy
  passed. The repository-wide Ruff format check is still blocked only by the
  pre-existing unformatted user changes in `workflows/kernel.py`. The canonical
  prompt digest remains
  `4c021a2e61df611a566c83c22fdf4378617314147de8dd6eaa9693160205ce27`.

## Queued Stage stale-result display correction

- The execution-result card now obeys its HTML `hidden` state even though its
  normal layout uses `display:flex`. A queued job with no Stage therefore no
  longer exposes the previous job's terminal failure card.
- Non-terminal renders also reset the result title, summary, metrics, retries,
  and package digest so stale terminal content cannot reappear during a later
  workspace or job transition.

## Single-instance and queue-worker hardening

- Studio now acquires an atomic, PID-owned lock inside the selected workspace
  before starting local model services. A second live instance fails closed,
  while a lock whose PID no longer exists is reclaimed safely.
- The HTTP server uses an exclusive Windows socket option so separate
  workspaces cannot silently share port 4173. Port conflicts return a stable
  startup error and clean up newly owned local services.
- Diagnostics expose `jobs.worker_alive`; new Stage 1 submissions fail with
  `UI198_WORKER_UNAVAILABLE` if the background worker is not alive. If the
  worker exits unexpectedly, queued/running jobs are terminally marked with the
  same stable code instead of remaining `QUEUED` forever.

## Stage 1 duplicate-segment retry correction

- The Studio failure showed `segment-greeting-003-01.json` rejected with
  `S166_SEGMENT_DUPLICATE`, followed by `S163_ZONE_RETRY_EXHAUSTED`. A duplicate
  retry previously received only generic novelty guidance at temperature 0.3.
- Duplicate retries now identify the exact rejected text, request a different
  event and opening sentence, and use temperature 0.7 for that retry. The
  deterministic duplicate gate and four-attempt limit remain in place.
- Verification: 33 Stage 1 integration tests pass; focused Ruff format/check,
  strict mypy, and `git diff --check` pass. The canonical prompt SHA-256 still
  matches `prompt.sha256` (`4c021a2e61df611a566c83c22fdf4378617314147de8dd6eaa9693160205ce27`).
- Repository-wide Ruff checks currently report unrelated existing formatting
  and import-order findings. The full pytest run was interrupted after it
  stopped producing progress; the focused Stage 1 suite completed cleanly.

## Stage 1 short-segment retry correction

- Inspected the failed local Studio transaction for
  `segment-introduction-001-02.json`: four responses failed as duplicate,
  missing terminal sentence punctuation, duplicate, then 19 words against a
  20-word minimum. Candidate artifacts and failure codes were preserved.
- Word-budget and sentence-punctuation retries now include the rejected text
  and ask the local model to revise that text while retaining the story event.
  Exact word deltas and final punctuation are stated explicitly. Deterministic
  validation, the bounded retry count, and committed artifacts remain intact.
- Verification: 33 Stage 1 integration tests pass; focused Ruff check/format
  and strict mypy pass.

## Stage 1 adaptive segment budgets

- The next Studio run failed at `segment-greeting-001-02.json`. Its candidate
  history contained a duplicate 27-word response, followed by the same
  19-word sentence on attempts 2–4. Repeated wording changes to retry feedback
  did not make the local model add words.
- Segment word ranges now account for the words already committed in the
  current item and the remaining segment capacity. A 19-word internal segment
  can pass when its neighboring segments bring the complete item into its
  existing deterministic word range. The segment ceiling scales with the
  profile's required item length; final item and zone validation remain intact.
- A regression case covers a 27 + 19 + 20 word item. Focused Stage 1 tests,
  Ruff, and mypy were rerun after the change.

## Stage 1 constrained-text padding correction

- Inspected all four saved candidates for the failed
  `segment-opening-004-02.json` transaction. After a duplicate, the local
  model appended `:25 words` and long emoji sequences to otherwise short
  sentences. These candidates failed the sentence-ending gate.
- The segment JSON schema had required at least 120 characters even when the
  deterministic word budget allowed a shorter complete sentence. The minimum
  is now one character, and the maximum scales with the active segment word
  budget. Deterministic word, punctuation, and duplicate gates still decide
  whether a candidate commits. Schema evidence version is now 1.3.
- Verification: 34 Stage 1 integration tests pass; focused Ruff format/check
  and strict mypy pass.

## Stage 1 item completion with bounded extra segments

- Inspected the failed `segment-greeting-001-03.json` candidates. The first
  two committed segments contained 18 and 16 words; every third-segment
  attempt returned the same 19-word sentence. Three such segments total only
  53 words, below the item's 60-word minimum.
- Stage 1 now accepts individually valid short segments and requests another
  segment while the complete item remains below its required word count. The
  number of segments is bounded from the item's minimum and a 15-word floor.
  Each segment retains duplicate, sentence, and word gates; the assembled item
  still passes the existing deterministic item and zone validators.
- Added a regression for 18 + 16 + 19 + 19 words. Focused Stage 1 integration
  tests, Ruff, and mypy pass.

## Stage 1 removes arbitrary per-segment word floor

- The next local failure at `segment-greeting-004-02.json` showed three
  identical 14-word, complete-sentence candidates rejected against the
  internal 15-word floor. The preceding committed segment had 18 words.
- A segment now needs at least one Unicode word and remains capped by the
  item's remaining maximum. The complete item's unchanged minimum and maximum
  are checked after assembly. A bounded segment count derived from the item
  minimum permits short but valid sentences without unbounded generation.
- The regression for extra segments now includes a 14-word sentence. All 35
  Stage 1 integration tests, focused Ruff checks, and strict mypy pass.

## Stage 1 character VLM output and single-person reference

- The Stage 1 run reached `char_001.png` after committing 185 of 186 assets.
  Replaying the retained ComfyUI PNG with the local Qwen2.5-VL model showed
  `status: FAIL` and a single string in `observable_findings`: two people were
  visible. The image was inspected and indeed contained two people. The Studio
  parser had required a list, converting this valid semantic rejection into
  `VLM005_INVALID_OUTPUT` and leaving the image generation call RUNNING.
- The VLM boundary now accepts a non-empty single finding as a one-element
  tuple while keeping PASS/FAIL, digest, and non-empty checks. Its error class
  is an image-adapter error so the image transaction records a failed attempt
  and can retry within its existing bound. The Stage 1 character prompt now
  requires exactly one person and excludes multiple/side-by-side figures.
- A live local smoke on the same character record produced one person, Qwen
  returned PASS, and the image became AUTHORITATIVE. The original two-person
  image correctly returned FAIL through the updated parser. A trial change to
  the VLM runner prompt broke its exact JSON keys and was reverted.
- Verification: 54 focused image, VLM, and Stage 1 tests pass; Ruff and strict
  mypy pass. Canonical prompt and `story.zip` layout remain unchanged.

## Stage 1 duplicate-character retry budget

- A Studio Stage 1 run on 2026-09-16 reached character generation after 185
  committed text artifacts. Both available seed-varied candidates for
  `char_001.png` were rejected by the local semantic gate with
  `IMG018_SEMANTIC_GATE_FAIL`; no candidate was committed or made authoritative.
- The production character-reference transaction now has three bounded,
  seed-varied attempts (rather than two). The third attempt remains subject to
  the same deterministic PNG checks and local VLM gate, and a failed candidate
  is still never bound. This addresses stochastic duplicate-person output
  without weakening image authority.
- The Studio execution view now reports only attempts actually recorded. It no
  longer renders an invented `1/3` retry total for transactions whose retry
  policy is not present in the runtime snapshot.

## Lower SDXL generation sizes across image stages

- The local ComfyUI adapter now generates Stage 1 at 896×1152, Stage 2 at
  1536×864, and Stage 3 at 768×1344. It center crops where needed and uses
  local RGB Lanczos resizing to retain the required final canvases of
  1536×2048, 3840×2160, and 1080×1920. Stage 4 renders video from earlier
  image assets and has no separate image generation step.
- Stage 2's 1536×864 source meets the canonical minimum 40% source edge ratio
  and maximum 2.5× linear managed upscale. The original proposed 1344×768
  source would have exceeded that limit. The canonical prompt and archive
  layout were not changed.
- The adapter embeds source PNG and decoded pixel digests, source dimensions,
  crop box, final dimensions, and resize method. The Stage 1 binder retains
  this evidence; Stage 2 and Stage 3 commitments record the source as
  `MANAGED_UPSCALED` with actual source dimensions and pixel digest.
- Verification: 37 related unit and integration tests pass. A sequential live
  ComfyUI smoke on the local SDXL model produced all three exact final sizes:
  Stage 1 896×1152 → 1536×2048 in 14.2 s, Stage 2 1536×864 → 3840×2160 in
  13.1 s, and Stage 3 768×1344 → 1080×1920 in 9.4 s. The full image quality
  improvement has not been measured; existing semantic and deterministic
  gates still decide whether a generated image is committed.
- Focused Ruff format/check, strict mypy, canonical prompt SHA-256, and Git
  whitespace checks pass. The repository-wide Ruff gates still report
  pre-existing Studio formatting/lint findings in `instance_lock.py`,
  `server.py`, `kernel.py`, and `test_studio.py`. The full pytest run was
  interrupted after progressing past 70%; the 37 related tests completed.

## Studio new-story metadata binding

- The dialog now submits separate episode title, series name, and episode
  number. Stage 1 binds these values into `story.json` in both production and
  deterministic test materialization; omitted series retains the prior title
  fallback. Without an explicit title, presets use their name without the UI
  index/prefix, while topic/brief/idea use the entered text and random mode
  gets a seed-specific label. Profile-specific age/family choices enter the creative
  instruction, and the UI's Stage 1 tool schema accepts the new fields.
- The existing series anchor builder only creates episode-one continuity.
  Continue/finalize choices are disabled in the dialog and rejected at the
  command boundary instead of silently writing episode 1. A new serial must
  start at episode 1; this change does not implement anchor continuation.
- Verification: 81 focused unit/integration tests pass, including reopening
  a package and asserting its `title`, `series`, and `episode`. Targeted Ruff
  check/format, strict mypy, and JavaScript syntax check pass. The canonical
  prompt SHA-256 matches `canonical/prompt.sha256`; archive layout is unchanged.

## Studio inferred series title

- When the optional series-name field is blank, Stage 1 now derives a reusable
  series name from the completed outline and script rather than copying the
  episode title. Explicit series names remain authoritative. The dialog makes
  this behavior clear and sends an absent series name as `null`.
- Verification: 51 focused Stage 1 and Studio-command tests pass; targeted
  Ruff format/check, strict mypy, JavaScript syntax check, and the canonical
  prompt SHA-256 check pass. The canonical prompt and `story.zip` layout are
  unchanged.

## Stage 1 dramatic-authoring reinforcement

- The bounded local segment context now carries a bilingual dramatic contract
  derived from the canonical narrative-architecture requirements. It requires
  a concrete want, disruption, stakes, resistance, an irreversible climax
  choice with a cost, persistent consequence, and a setup/payoff closing
  image. It also bans repeated arrival, decoration, nostalgia, or mystery
  beats that do not change story state.
- This is a generation-time instruction only: it does not alter the canonical
  prompt, `story.json` schema, or `story.zip` layout, and it avoids imitating
  any individual screenwriter's distinctive voice.
- Verification: 73 focused Stage 1 unit/integration tests pass; targeted Ruff
  format/check and strict mypy pass using the project environments.

## Stage 3 ordered member digest correction

- `ordered_asset_manifest_digest_sha256` now hashes only the canonical ordered
  `[relative_path, exact_member_sha256]` tuples. It binds every final member
  except `workflow_manifest.json` and `package_quality_report.json`, never the
  ZIP container, compression metadata, or archive digest.
- Stage 3 reopen recomputes this exact projection from extracted members and
  fails if the quality report binds any other value.
- Verification: focused digest/report tests pass; targeted Ruff format/check,
  strict mypy and Git whitespace checks pass. The historical M7 intake tests
  remain incompatible with the currently uncommitted Stage 1 outline contract
  change and were not altered by this correction.
